import React from 'react';
import {View, Text} from 'react-native';
import Router from './routes';

const Source = () => <Router style={{flex: 1, backgroundColor: '#ccc'}} />;

export default Source;
